name: Keep Render Alive

on:
  schedule:
    - cron: "*/14 * * * *"  # every 14 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET request
        run: curl -s https://aurevox-lens.onrender.com/ > /dev/null
